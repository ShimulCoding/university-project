Each module is intentionally separated into its own route, controller, service, and types entry point.
As business logic is added, keep validation and repository logic inside the same module folder rather than expanding one shared file.

